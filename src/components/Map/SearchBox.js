import React from 'react'
// UI
import { TextField } from 'material-ui'

export default (props) => (
  <div>
      {/* <TextField
        fullWidth={true}
        hint={'Type a place address or name here'}
        onChange={props.changeHandler}
        name={'search-box'}
        type={'text'}
        value={props.value}
      /> */}
  </div>
)

